# Staging-specific overrides for defineString()/defineSecret() params in
# functions/index.js. Loaded automatically by firebase-tools when deploying
# with --project cluttrd-staging, taking precedence over the base .env - so
# this never affects production's canary, which keeps reading .env's own
# CANARY_TEST_UID default.

# Dedicated staging test account, created specifically for
# analyzePhotoCanary to sign in as on the staging project (staging has its
# own separate Auth system - the base .env's default uid is a production
# account that doesn't exist here).
CANARY_TEST_UID=0eqbRaeCmOa8H6kfZyEQwguDOup1
