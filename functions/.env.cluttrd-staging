# Staging-specific overrides for defineString()/defineSecret() params in
# functions/index.js. Loaded automatically by firebase-tools when deploying
# with --project cluttrd-staging, taking precedence over the base .env - so
# this never affects production's canary, which keeps reading .env's own
# CANARY_TEST_UID default.

# Dedicated staging test account, created specifically for
# analyzePhotoCanary to sign in as on the staging project (staging has its
# own separate Auth system - the base .env's default uid is a production
# account that doesn't exist here).
# Repointed 2026-07-18 from 0eqbRaeCmOa8H6kfZyEQwguDOup1: that uid lost its
# email/password at some point (cause not fully traced) and got reborn as a
# bare shadow record via this exact canary's own custom-token sign-in, while
# the real canary-staging@uncluttrd.app credentials ended up on this uid
# instead. Standardizing on the uid that's actually reachable by email/
# password, not the orphaned one - see DecisionLog.md if this needs the full
# investigation trail. The old uid is left alone, unused but harmless.
CANARY_TEST_UID=8hD3iwG3MYOrbfww2g4QsVwHFsr1

# Staging's own Web API key (project cluttrd-staging, app
# 1:247455199173:web:b3c11631fd4aa4770965ac). Without this override,
# analyzePhotoCanary would exchange a staging-minted custom token against
# production's Identity Toolkit using the base .env's key, which fails on
# project mismatch.
CANARY_WEB_API_KEY=AIzaSyDgBFIR35WcYYllxuGVNDmfGGMt4Hq71E4
